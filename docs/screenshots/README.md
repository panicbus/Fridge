Populate this folder before `npm run render:booklet` (images are omitted from git):

| Save as               | Typical source                            |
|-----------------------|--------------------------------------------|
| `home.png`            | Home / ingredient bag screen               |
| `results.png`         | Recipe results grid                        |
| `recipe-detail.png`   | Single recipe detail (reserved for edits) |
| `cook-mode.png`       | Cooking mode                               |
| `pantry.png`          | Pantry manage (reserved for edits)       |

The welcome booklet references `home.png`, `results.png`, and `cook-mode.png` today.
