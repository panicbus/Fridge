{
  "name": "fridge-prep-recipes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "download": "node download.js",
    "filter": "node filter.js",
    "build-db": "node build-db.js",
    "fetch-images": "node fetch-images.js",
    "audit-diet": "node --import tsx audit-diet.js",
    "build-data": "npm run download && npm run filter && npm run build-db",
    "all": "npm run download && npm run filter && npm run build-db && npm run fetch-images"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "cheerio": "^1.0.0",
    "csv-parser": "^3.0.0",
    "node-fetch": "^3.3.2",
    "p-limit": "^5.0.0",
    "sharp": "^0.33.0"
  }
}
